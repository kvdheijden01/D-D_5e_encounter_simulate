<%= form_with model: creature do |form| %>
  <div>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
    <% creature.errors.full_messages_for(:name).each do |message| %>
      <div><%= message %></div>
    <% end %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>