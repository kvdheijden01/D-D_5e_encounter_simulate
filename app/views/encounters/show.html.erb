<h1>Encounter</h1>

<h2>Enemies</h2>

<ul>
    <% @encounters.enemy.creatures.each do |creature| %>
        <li>
        <%= creature.creaturetype.name %>
        </li>
    <% end %>

</ul>

<%= button_to "Add creature", action: "add", params: { id: @encounters.id } %>